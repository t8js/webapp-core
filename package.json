{
  "name": "@t8/webapp-core",
  "version": "0.1.12",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist', { force: true, recursive: true });\"",
    "compile": "npx esbuild index.ts --bundle --outdir=dist --platform=node --format=esm --external:express",
    "preversion": "npx npm-run-all clean shape compile",
    "shape": "npx codeshape --typecheck --emit-types"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/t8js/webapp-core.git"
  },
  "author": "axtk",
  "license": "MIT",
  "peerDependencies": {
    "express": ">=5"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.0"
  },
  "dependencies": {
    "@t8/date-format": "^1.0.15"
  }
}
